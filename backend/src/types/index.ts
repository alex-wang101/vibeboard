// Re-export all shared types for convenience within the backend
export type {
  ArchitectureGraph,
  ArchitectureNode,
  NodeType,
  ExecutionContext,
  FileInfo,
  ImportInfo,
  NodeMetadata,
  FlowEdge,
  PaletteItem,
  Project,
  CreateProjectRequest,
  ScanRequest,
  ScanResponse,
  GitHubRepo,
} from '@vibeboard/shared';
